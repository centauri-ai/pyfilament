apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
- includeSelectors: true
  includeTemplates: true
  pairs:
    app: pyfilament-api

resources:
- deployment.yaml
- service.yaml
images:
- name: ghcr.io/centauri-ai/pyfilament
  newTag: 0.1.0
